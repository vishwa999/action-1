name: My first workflow
on: push
jobs:
    first_job:
        runs-on: ubuntu-latest
        steps:
            - name: Welcome message
              run: echo " My first github action"

            - name: list files
              run: ls
              
            - name: read the file
              run: cat README.md
              
            
